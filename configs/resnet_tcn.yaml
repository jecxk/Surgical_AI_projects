# ResNet50 + TCN Configuration

model:
  backbone: "resnet50"
  feature_dim: 2048
  temporal_model: "tcn"
  dropout: 0.3
  
  tcn:
    num_stages: 3
    num_layers_per_stage: 10
    num_filters: 64

training:
  batch_size: 8
  epochs: 50
  optimizer:
    lr: 1.0e-4
    backbone_lr: 1.0e-5
